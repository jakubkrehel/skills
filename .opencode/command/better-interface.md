---
description: Holistic cross-discipline interface review of a screen, flow, feature, or product. Pass an optional mode and scope, e.g. "quick" or "full checkout flow".
---

Invoke the skill tool to load the skill named `better-interface`, then run the review exactly as that skill defines it, including its mode parsing, shared severity, finding cap, output format, and verdict. The invocation is: $ARGUMENTS. When no mode is present, default to `full`. When no scope is present, infer it from the current workspace as the skill instructs. Treat the review as read-only unless the user also asks to implement the findings.